import { NumeroElementosPipe } from './numero-elementos.pipe';

describe('NumeroElementosPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroElementosPipe();
    expect(pipe).toBeTruthy();
  });
});
